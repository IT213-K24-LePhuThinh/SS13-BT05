# SS13-BT05 - Xây Dựng Hệ Thống Autonomous Incident Responder & Tracing

## 1. Sơ đồ luồng tương tác (ASCII Flow Diagram)

Xem đầy đủ tại `docs/architecture-flow-diagram.txt`. Chu trình khép kín 5 giai đoạn:

```
Incident Ingestion (Đọc log)
        -> Data Sanitization (Lọc SQL & Path)
        -> DB Audit (Đối soát Postgres)
        -> Archival & Reporting (Nén log & Ghi file)
        -> LLMOps Observability (Langfuse Tracing Gateway)
```

Ranh giới giữa API Client, Spring AI ChatClient, Postgres MCP, FileSystem MCP,
Custom Archive Tool và Langfuse Tracing Gateway được vẽ rõ trong sơ đồ.

## 2. AutonomousLogisticsAgentService.java

- Workflow khép kín tái sử dụng toàn bộ thành phần Bài 1-4.
- Phân loại mức độ nghiêm trọng:
  - `CRITICAL`: trên 5 đơn trễ HOẶC lỗi COD > 10.000.000 VNĐ.
  - `MEDIUM`: từ 2 đơn trễ hoặc lỗi COD > 2.000.000 VNĐ.
  - `LOW`: trường hợp còn lại.
- Nếu CRITICAL, tự động tạo mục khuyến nghị hành động khẩn cấp trong báo cáo.

System Prompt cấu hình 5 giai đoạn autonomous (hằng số `AUTONOMOUS_SYSTEM_PROMPT`).

## 3. Cấu hình Tracing & Giám sát LLMOps

`LLMObsTracingConfig`:
- Micrometer Tracing bridge OpenTelemetry (`micrometer-tracing-bridge-otel`).
- OTel SDK exporter OTLP HTTP -> Langfuse gateway `http://localhost:4340/v1/traces`.
- `ToolTracingAdvisor` (Advisor) đo cho mỗi tool call:
  - độ trễ (latency) từng tool,
  - số token tiêu thụ (từ usage response),
  - ghi status `ERROR` nếu tool thất bại.

## 4. Thực nghiệm chạy thực tế & Log minh chứng

- Kịch bản dữ liệu log sự cố: `evidence/incident-log-input.txt`
  (10 sự cố SG-02: 7 đơn trễ + 13.200.000 VNĐ COD thất bại).
- Conversation Log toàn bộ chu trình: `evidence/conversation-full-loop-console.log`.
- Báo cáo Markdown CRITICAL tự sinh: `evidence/CRITICAL_incident_audit.md`.

Kết quả: hệ thống tự kích hoạt cờ CRITICAL đúng ngưỡng, hoàn tất 4 tool call
trong 4 iterations (dưới giới hạn 8), ghi báo cáo + nén log + đẩy trace lên Langfuse.

```
AUTONOMOUS INCIDENT RUN COMPLETED | severity=CRITICAL | delayed=7 | codFailureVnd=13200000.0
{
  "severity": "CRITICAL",
  "reportFilePath": "C:/data/logistics/reports/SG-02_incident_audit.md",
  "archiveStatus": "SUCCESS",
  "recommendation": "KHẨN CẤP: điều thêm 3 shipper, liên hệ ngay khách hàng, đổi tuyến giao tối ưu"
}
```